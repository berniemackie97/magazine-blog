---
interface Props {
  accent?: string;
  padded?: boolean;
}

const { accent, padded = true } = Astro.props;
const frameStyle = `${accent ? `--accent:${accent};` : ''}${!padded ? 'padding:0;' : ''}`;
---
<div style={frameStyle || undefined} class="frame-panel relative">
  <slot />
</div>
