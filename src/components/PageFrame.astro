---
interface Props {
  accent?: string;
  padded?: boolean;
}

const { accent, padded = true } = Astro.props;
---
<div style={accent ? `--accent: ${accent}` : undefined} class={`relative ${padded ? 'p-[var(--page-pad)]' : ''}`}>
  <slot />
</div>
